# Marvish Chandra

# Press ⌃R to execute it or replace it with your code.
# Press Double ⇧ to search everywhere for classes, files, tool windows, actions, and settings.

import Math


def gatewayShape(A,B,C,h,x):
    A = .69
    catenary = 1 / A # catenary being the calculated B constant
    y = A * Math.cos * h(catenary * x) + C
